# 多标签页支持 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ChatArea 支持同时打开多个会话、FileEditor 支持同时打开多个文件，各自通过独立的 TabBar 管理标签页。

**Architecture:** 新增通用 TabBar 组件，扩展 Zustand store 管理标签状态和消息缓存，重构 ChatArea 的流式事件路由按 session_id 分发，FileEditor 改为多 DOM 容器 + CodeMirror 实例缓存。移除旧的单视图字段（activeSessionTitle、selectedFilePath 等）。

**Tech Stack:** React 18 + TypeScript 5，Zustand v5，CodeMirror 6，Tailwind CSS v4，@wailsio/runtime

---

### Task 1: Store — 新增类型定义和新状态

**Files:**
- Modify: `frontend/src/store/index.ts` (types + initial state section)

- [ ] **Step 1: 新增 TabInfo 和 FileTabInfo 类型定义，扩展 AppState**

在 `Message` 接口之后、`AppState` 接口之前，添加新类型定义。更新 `AppState` 接口新增/移除字段。

```typescript
// 在 Message 接口之后添加：

interface SessionTabInfo {
  id: string
  title: string
}

interface FileTabInfo {
  path: string
  content: string
  isDirty: boolean
}

// 更新 AppState 接口 — 替换现有单视图字段为新字段：

interface AppState {
  // 保留：messages（用于渲染的当前活跃视图消息）
  messages: Message[]
  // 移除：generating: boolean，替换为 generatingSessionId
  generatingSessionId: string
  tokenCount: number
  projectPath: string
  branch: string
  // 移除：activeSessionTitle
  activeSessionId: string
  consoleLines: string[]
  layoutMode: LayoutMode
  splitRatio: number
  // 移除：selectedFilePath, selectedFileContent
  activeFilePath: string

  // 新增 — 会话标签
  openSessions: SessionTabInfo[]
  sessionMessages: Record<string, Message[]>

  // 新增 — 文件标签
  openFiles: FileTabInfo[]

  // ... actions（后续 tasks 逐步添加）
}
```

- [ ] **Step 2: 更新初始状态**

在 `create<AppState>((set) => ({` 中替换/新增默认值。

```typescript
export const useStore = create<AppState>((set) => ({
  messages: [{ id: 'welcome', role: 'system', content: 'Welcome to Monika. Type /help for commands.' }],
  generatingSessionId: '',     // 替换 generating: false
  tokenCount: 0,
  projectPath: '',
  branch: '',
  activeSessionId: '',
  // 移除 activeSessionTitle: ''
  consoleLines: ['$ ready'],
  layoutMode: 'split',
  splitRatio: 0.5,
  activeFilePath: '',          // 替换 selectedFilePath + selectedFileContent
  // 移除 selectedFileContent: ''

  // 新增
  openSessions: [],
  sessionMessages: {},
  openFiles: [],

  // ... 现有 actions 保留，后续 tasks 中逐步替换/新增
}))
```

- [ ] **Step 3: 删除旧字段的相关 actions**

移除以下 action 实现（后续 tasks 会用新操作替代）：
- `setActiveSessionTitle` — 整个函数移除
- `setSelectedFile` — 整个函数移除  
- `clearSelectedFile` — 整个函数移除

在 `AppState` 接口中也移除对应方法声明。

- [ ] **Step 4: 验证编译**

Run: `cd frontend && npx tsc --noEmit`
Expected: 错误仅来自引用已移除字段的组件（ChatArea、FileEditor、SessionList），这些在后续 tasks 中修复。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/store/index.ts
git commit -m "feat(store): add multi-tab types and state, remove single-view fields"
```

---

### Task 2: Store — 会话标签操作

**Files:**
- Modify: `frontend/src/store/index.ts` (actions section)

- [ ] **Step 1: 添加会话标签操作**

在 `AppState` 接口中添加以下方法声明，然后在 `create()` 回调中实现。

```typescript
// AppState 接口中：
openSessionTab: (id: string, title: string) => Promise<void>
closeSessionTab: (id: string) => void
switchSessionTab: (id: string) => void
setGeneratingSessionId: (id: string) => void
clearGeneratingSessionId: () => void

// create() 回调中：
openSessionTab: async (id, title) => {
  const state = useStore.getState()
  const existing = state.openSessions.find((s) => s.id === id)
  if (existing) {
    // 已存在 — 仅切换
    state.switchSessionTab(id)
    return
  }
  if (state.openSessions.length >= 8) {
    state.addMessage({ id: crypto.randomUUID(), role: 'error', content: 'Too many sessions open. Close one first.' })
    return
  }
  // 加入列表
  set((s) => ({
    openSessions: [...s.openSessions, { id, title }],
    activeSessionId: id,
  }))
  // 从后端加载消息
  try {
    const session = await App.LoadSession(state.projectPath, id)
    const msgs = session.messages ? loadSessionMessages(session.messages as any[]) : []
    set((s) => ({
      sessionMessages: { ...s.sessionMessages, [id]: msgs },
      messages: msgs,
    }))
  } catch {
    set((s) => ({
      sessionMessages: { ...s.sessionMessages, [id]: [] },
      messages: [],
    }))
  }
},

closeSessionTab: (id) => {
  set((s) => {
    const idx = s.openSessions.findIndex((t) => t.id === id)
    const next = [...s.openSessions]
    next.splice(idx, 1)
    const msgCache = { ...s.sessionMessages }
    delete msgCache[id]

    let newActive = s.activeSessionId
    if (id === s.activeSessionId) {
      if (idx < next.length) newActive = next[idx].id        // 右侧邻居
      else if (next.length > 0) newActive = next[next.length - 1].id  // 左侧最后一个
      else newActive = ''                                     // 无剩余
    }

    const newMessages = newActive ? (msgCache[newActive] || []) : [{ id: 'welcome', role: 'system', content: 'Welcome to Monika.' }]

    return {
      openSessions: next,
      sessionMessages: msgCache,
      activeSessionId: newActive,
      messages: newMessages,
    }
  })
},

switchSessionTab: (id) => {
  set((s) => {
    if (id === s.activeSessionId) return {}
    // 写回当前 messages 到缓存
    const msgCache = { ...s.sessionMessages, [s.activeSessionId]: s.messages }
    const restored = msgCache[id] || []
    return {
      activeSessionId: id,
      sessionMessages: msgCache,
      messages: restored,
    }
  })
},

setGeneratingSessionId: (id) => set({ generatingSessionId: id }),
clearGeneratingSessionId: () => set({ generatingSessionId: '' }),
```

- [ ] **Step 2: 验证编译**

Run: `cd frontend && npx tsc --noEmit`
Expected: 新增操作的类型应通过编译。

- [ ] **Step 3: Commit**

```bash
git add frontend/src/store/index.ts
git commit -m "feat(store): add session tab operations"
```

---

### Task 3: Store — 文件标签操作

**Files:**
- Modify: `frontend/src/store/index.ts` (actions section)

- [ ] **Step 1: 添加文件标签操作**

```typescript
// AppState 接口中：
openFileTab: (path: string, content: string) => void
closeFileTab: (path: string) => void
switchFileTab: (path: string) => void
setFileDirty: (path: string, dirty: boolean) => void
updateFileContent: (path: string, content: string) => void

// create() 回调中：
openFileTab: (path, content) => {
  const state = useStore.getState()
  const existing = state.openFiles.find((f) => f.path === path)
  if (existing) {
    state.switchFileTab(path)
    return
  }
  set((s) => ({
    openFiles: [...s.openFiles, { path, content, isDirty: false }],
    activeFilePath: path,
  }))
},

closeFileTab: (path) => {
  set((s) => {
    const idx = s.openFiles.findIndex((f) => f.path === path)
    const next = [...s.openFiles]
    next.splice(idx, 1)

    let newActive = s.activeFilePath
    if (path === s.activeFilePath) {
      if (idx < next.length) newActive = next[idx].path
      else if (next.length > 0) newActive = next[next.length - 1].path
      else newActive = ''
    }

    return { openFiles: next, activeFilePath: newActive }
  })
},

switchFileTab: (path) => {
  set((s) => {
    if (path === s.activeFilePath) return {}
    return { activeFilePath: path }
  })
},

setFileDirty: (path, dirty) => {
  set((s) => ({
    openFiles: s.openFiles.map((f) => f.path === path ? { ...f, isDirty: dirty } : f),
  }))
},

updateFileContent: (path, content) => {
  set((s) => ({
    openFiles: s.openFiles.map((f) => f.path === path ? { ...f, content } : f),
  }))
},
```

- [ ] **Step 2: 验证编译**

Run: `cd frontend && npx tsc --noEmit`

- [ ] **Step 3: Commit**

```bash
git add frontend/src/store/index.ts
git commit -m "feat(store): add file tab operations"
```

---

### Task 4: Store — Per-session 流式事件操作

**Files:**
- Modify: `frontend/src/store/index.ts` (actions + setupWailsEvents)

- [ ] **Step 1: 添加 per-session store actions**

```typescript
// AppState 接口中：
updateSessionMessage: (id: string, delta: string) => void
updateSessionThinking: (id: string, delta: string) => void
addSessionToolStart: (id: string, tool: ToolCall) => void
updateSessionToolDone: (id: string, name: string, output: string, status: 'done' | 'error') => void

// create() 回调中：
updateSessionMessage: (id, delta) => {
  set((s) => {
    const msgs = [...(s.sessionMessages[id] || [])]
    for (let i = msgs.length - 1; i >= 0; i--) {
      if (msgs[i].role === 'assistant') {
        msgs[i] = { ...msgs[i], content: msgs[i].content + delta }
        break
      }
    }
    return { sessionMessages: { ...s.sessionMessages, [id]: msgs } }
  })
},

updateSessionThinking: (id, delta) => {
  set((s) => {
    const msgs = [...(s.sessionMessages[id] || [])]
    for (let i = msgs.length - 1; i >= 0; i--) {
      if (msgs[i].role === 'assistant') {
        msgs[i] = { ...msgs[i], thinking: (msgs[i].thinking || '') + delta }
        break
      }
    }
    return { sessionMessages: { ...s.sessionMessages, [id]: msgs } }
  })
},

addSessionToolStart: (id, tool) => {
  set((s) => {
    const msgs = [...(s.sessionMessages[id] || [])]
    for (let i = msgs.length - 1; i >= 0; i--) {
      if (msgs[i].role === 'assistant') {
        msgs[i] = { ...msgs[i], tools: [...(msgs[i].tools || []), tool] }
        break
      }
    }
    return { sessionMessages: { ...s.sessionMessages, [id]: msgs } }
  })
},

updateSessionToolDone: (id, name, output, status) => {
  set((s) => {
    const msgs = [...(s.sessionMessages[id] || [])]
    for (let i = msgs.length - 1; i >= 0; i--) {
      if (msgs[i].role === 'assistant' && msgs[i].tools) {
        msgs[i] = {
          ...msgs[i],
          tools: msgs[i].tools!.map((t) =>
            t.name === name && t.status === 'running' ? { ...t, output, status } : t
          ),
        }
        break
      }
    }
    return { sessionMessages: { ...s.sessionMessages, [id]: msgs } }
  })
},
```

- [ ] **Step 2: 重构 setupWailsEvents 使用 per-session 路由**

替换 `Events.On('stream', ...)` 处理器，使用 `data.session_id` 路由事件。

```typescript
export function setupWailsEvents() {
  console.log('[monika] setupWailsEvents: subscribing to stream')
  Events.On('stream', (ev) => {
    const store = useStore.getState()
    const data = ev.data as StreamEvent
    const sid = data.session_id

    // Shadow path: 无 session_id 或 session 已关闭
    if (!sid || !store.sessionMessages[sid]) {
      console.warn('[monika] stream event dropped: no session_id or session closed', data.type)
      return
    }

    switch (data.type) {
      case 'text_delta':
        store.updateSessionMessage(sid, data.content || '')
        if (sid === store.activeSessionId) {
          store.updateLastAssistant(data.content || '')
        }
        break

      case 'thinking':
        store.updateSessionThinking(sid, data.content || '')
        if (sid === store.activeSessionId) {
          store.updateLastAssistantThinking(data.content || '')
        }
        break

      case 'tool_start':
        if (data.tool) {
          store.addSessionToolStart(sid, { name: data.tool.name, input: data.tool.input || '', status: 'running' })
          store.addConsoleLine(`$ ${data.tool.name} ${data.tool.input || ''}`)
          if (sid === store.activeSessionId) {
            store.addToolStart({ name: data.tool.name, input: data.tool.input || '', status: 'running' })
          }
        }
        break

      case 'tool_output':
        if (data.tool) {
          store.addConsoleLine(data.tool.output || '')
        }
        break

      case 'tool_done':
        if (data.tool) {
          const status = data.tool.status as 'done' | 'error' || 'done'
          store.updateSessionToolDone(sid, data.tool.name, data.tool.output || '', status)
          store.addConsoleLine(`[${status}] ${data.tool.name}`)
          if (sid === store.activeSessionId) {
            store.updateToolDone(data.tool.name, data.tool.output || '', status)
          }
        }
        break

      case 'usage':
        if (data.usage) {
          store.addTokens(data.usage.total_tokens || 0)
        }
        break

      case 'error':
        // 后台错误 — 仅写入缓存，不污染当前活跃视图的 messages
        store.updateSessionMessage(sid, '')
        if (sid === store.activeSessionId) {
          store.addMessage({ id: crypto.randomUUID(), role: 'error', content: data.content || 'Unknown error' })
        }
        store.addConsoleLine(`[error] ${data.content || 'Unknown error'}`)
        if (sid === store.generatingSessionId) {
          store.clearGeneratingSessionId()
        }
        break

      case 'file_changed':
        if (data.file_change) {
          store.addConsoleLine(`[file] ${data.file_change.path} ${data.file_change.status}`)
        }
        break

      case 'done':
        if (sid === store.generatingSessionId) {
          store.clearGeneratingSessionId()
        }
        if (sid === store.activeSessionId) {
          store.setGenerating(false)  // 兼容 ChatInput disabled
        }
        break
    }
  })
}
```

注意：`setGenerating` 调用需要保留以便在 active session 完成时更新 ChatInput disabled 状态。更干净的做法是 ChatInput 直接读取 `generatingSessionId`，但这个改动较小，保持现有 `generating` setter 作为派生状态，在 `done` 且 `sid === activeSessionId` 时调用。

- [ ] **Step 3: 验证编译**

Run: `cd frontend && npx tsc --noEmit`

- [ ] **Step 4: Commit**

```bash
git add frontend/src/store/index.ts
git commit -m "feat(store): add per-session stream actions, refactor setupWailsEvents"
```

---

### Task 5: TabBar 组件 — 基础结构

**Files:**
- Create: `frontend/src/components/TabBar/TabBar.tsx`

- [ ] **Step 1: 创建 TabBar 组件文件**

```typescript
import { useState, useRef, useEffect, useCallback } from 'react'
import { IconClose } from '../Icons'

interface TabData {
  key: string
  label: string
  dirty?: boolean
  status?: 'idle' | 'generating' | 'completed' | 'error'
}

interface TabBarProps {
  tabs: TabData[]
  activeKey: string
  onSelect: (key: string) => void
  onClose: (key: string) => void
  onReorder?: (from: number, to: number) => void
  emptyLabel: string
}

function StatusIndicator({ status }: { status?: TabData['status'] }) {
  if (!status || status === 'idle') return null

  if (status === 'generating') {
    return (
      <span
        className="inline-block w-[6px] h-[6px] rounded-full mr-1 flex-shrink-0 animate-pulse"
        style={{ backgroundColor: 'var(--yellow)' }}
      />
    )
  }
  if (status === 'completed') {
    return (
      <span className="text-[10px] mr-1 flex-shrink-0" style={{ color: 'var(--green)' }}>{'✓'}</span>
    )
  }
  if (status === 'error') {
    return (
      <span
        className="inline-block w-[6px] h-[6px] rounded-full mr-1 flex-shrink-0"
        style={{ backgroundColor: 'var(--red)' }}
      />
    )
  }
  return null
}

function TabBar({ tabs, activeKey, onSelect, onClose, emptyLabel }: TabBarProps) {
  const containerRef = useRef<HTMLDivElement>(null)

  if (tabs.length === 0) {
    return (
      <div
        className="flex items-center px-3 py-1 border-b border-[var(--border)] flex-shrink-0"
        style={{ background: 'var(--glass-strong)', height: 36 }}
      >
        <span className="text-[12px] text-[var(--text-dim)]">{emptyLabel}</span>
      </div>
    )
  }

  return (
    <div
      ref={containerRef}
      className="flex items-end border-b border-[var(--border)] flex-shrink-0 overflow-x-auto"
      style={{ background: 'var(--glass-strong)', height: 36 }}
      role="tablist"
      aria-orientation="horizontal"
    >
      {tabs.map((tab) => {
        const isActive = tab.key === activeKey
        return (
          <div
            key={tab.key}
            role="tab"
            aria-selected={isActive}
            aria-label={`${tab.label}${tab.dirty ? ' (unsaved)' : ''}${tab.status === 'generating' ? ' (generating)' : ''}`}
            tabIndex={isActive ? 0 : -1}
            onClick={() => onSelect(tab.key)}
            onKeyDown={(e) => {
              if (e.key === 'Enter' || e.key === ' ') {
                e.preventDefault()
                onSelect(tab.key)
              } else if (e.key === 'w' && e.ctrlKey) {
                e.preventDefault()
                onClose(tab.key)
              }
            }}
            className="flex items-center gap-1 px-2 cursor-pointer transition-colors border-r border-[var(--border)] select-none flex-shrink-0"
            style={{
              minWidth: 120,
              maxWidth: 200,
              height: 35,
              background: isActive ? 'var(--bg-main)' : 'transparent',
              color: isActive ? 'var(--text-primary)' : 'var(--text-secondary)',
            }}
          >
            <StatusIndicator status={tab.status} />
            <span className="text-[12px] truncate flex-1">{tab.label}</span>
            {tab.dirty && (
              <span className="text-[8px] flex-shrink-0" style={{ color: 'var(--text-dim)' }}>{'●'}</span>
            )}
            <button
              onClick={(e) => { e.stopPropagation(); onClose(tab.key) }}
              aria-label={`Close ${tab.label}`}
              className="text-[var(--text-dim)] hover:text-[var(--text-primary)] hover:bg-[var(--glass-hover)] w-4 h-4 flex items-center justify-center rounded flex-shrink-0 transition-colors"
              style={{ opacity: isActive ? 1 : 0 }}
              onMouseEnter={(e) => { if (!isActive) (e.currentTarget as HTMLElement).style.opacity = '1' }}
              onMouseLeave={(e) => { if (!isActive) (e.currentTarget as HTMLElement).style.opacity = '0' }}
            >
              <IconClose size={10} />
            </button>
          </div>
        )
      })}
    </div>
  )
}

export default TabBar
```

- [ ] **Step 2: 验证编译**

Run: `cd frontend && npx tsc --noEmit`

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/TabBar/TabBar.tsx
git commit -m "feat(TabBar): add basic tab bar component with keyboard nav and ARIA"
```

---

### Task 6: TabBar — 溢出下拉菜单

**Files:**
- Modify: `frontend/src/components/TabBar/TabBar.tsx`

- [ ] **Step 1: 添加溢出下拉菜单逻辑**

在 `TabBar` 函数内，`return` 之前添加 overflow 侦测和下拉菜单状态。替换基础组件 return 为带溢出的版本。

在文件顶部 import 中加入：
```typescript
import { IconChevronDown } from '../Icons'
```

在 `TabBar` 函数中 `containerRef` 之后添加：

```typescript
const [overflowKeys, setOverflowKeys] = useState<string[]>([])
const [menuOpen, setMenuOpen] = useState(false)
const tabRefs = useRef<Map<string, HTMLDivElement>>(new Map())
const menuRef = useRef<HTMLDivElement>(null)

// ResizeObserver 计算溢出
useEffect(() => {
  const el = containerRef.current
  if (!el || tabs.length === 0) return

  const calc = () => {
    const containerWidth = el.clientWidth
    const moreBtnWidth = 28
    let used = 0
    const overflow: string[] = []

    tabs.forEach((tab) => {
      const minW = 120
      if (used + minW + moreBtnWidth <= containerWidth) {
        used += minW
      } else {
        overflow.push(tab.key)
      }
    })

    // 给可见标签分配剩余空间
    const visibleCount = tabs.length - overflow.length
    if (visibleCount > 0) {
      const availableForVisible = containerWidth - (overflow.length > 0 ? moreBtnWidth : 0)
      const perTab = Math.min(200, availableForVisible / visibleCount)
      used = 0
      overflow.length = 0
      tabs.forEach((tab) => {
        if (used + 120 + (overflow.length > 0 ? moreBtnWidth : 0) <= containerWidth) {
          used += perTab
        } else {
          overflow.push(tab.key)
        }
      })
    }

    setOverflowKeys(overflow)
  }

  calc()
  const ro = new ResizeObserver(calc)
  ro.observe(el)
  return () => ro.disconnect()
}, [tabs])

// 点击外部关闭菜单
useEffect(() => {
  if (!menuOpen) return
  const handler = (e: MouseEvent) => {
    if (menuRef.current && !menuRef.current.contains(e.target as Node)) {
      setMenuOpen(false)
    }
  }
  document.addEventListener('mousedown', handler)
  return () => document.removeEventListener('mousedown', handler)
}, [menuOpen])

// 键盘关闭菜单
const handleMenuKey = useCallback((e: React.KeyboardEvent) => {
  if (e.key === 'Escape') { setMenuOpen(false); containerRef.current?.focus() }
}, [])
```

修改 tab 渲染中的可见性控制，并且在尾部添加 `▼` 下拉按钮和菜单：

```tsx
// 只渲染非 overflow 的标签（在 tabs.map 中添加条件）
{tabs.filter((t) => !overflowKeys.includes(t.key)).map((tab) => (
  // ... 现有 tab 渲染代码 ...
))}

{/* ▼ 溢出按钮 */}
{overflowKeys.length > 0 && (
  <div className="relative flex-shrink-0">
    <button
      onClick={() => setMenuOpen(!menuOpen)}
      aria-haspopup="menu"
      aria-expanded={menuOpen}
      className="flex items-center justify-center w-[28px] h-full text-[var(--text-dim)] hover:text-[var(--text-primary)] transition-colors"
    >
      <IconChevronDown size={12} />
    </button>
    {menuOpen && (
      <div
        ref={menuRef}
        role="menu"
        onKeyDown={handleMenuKey}
        className="absolute right-0 top-full bg-[var(--bg-titlebar)] border border-[var(--border)] rounded-[4px] shadow-lg z-50 min-w-[160px]"
        style={{ maxHeight: 400, overflowY: 'auto' }}
      >
        {tabs.filter((t) => overflowKeys.includes(t.key)).map((tab) => (
          <div
            key={tab.key}
            role="menuitem"
            onClick={() => { onSelect(tab.key); setMenuOpen(false) }}
            className="flex items-center gap-1 px-3 py-1.5 cursor-pointer text-[12px] hover:bg-[var(--glass-hover)] transition-colors"
            style={{
              color: tab.key === activeKey ? 'var(--text-primary)' : 'var(--text-secondary)',
            }}
          >
            <StatusIndicator status={tab.status} />
            <span className="flex-1 truncate">{tab.label}</span>
            {tab.key === activeKey && (
              <span className="text-[10px]" style={{ color: 'var(--accent)' }}>{'✓'}</span>
            )}
            {tab.dirty && (
              <span className="text-[8px] flex-shrink-0" style={{ color: 'var(--text-dim)' }}>{'●'}</span>
            )}
            <button
              onClick={(e) => { e.stopPropagation(); onClose(tab.key) }}
              aria-label={`Close ${tab.label}`}
              className="text-[var(--text-dim)] hover:text-[var(--text-primary)] w-4 h-4 flex items-center justify-center rounded flex-shrink-0 transition-colors ml-1"
            >
              <IconClose size={10} />
            </button>
          </div>
        ))}
      </div>
    )}
  </div>
)}
```

- [ ] **Step 2: 验证编译**

Run: `cd frontend && npx tsc --noEmit`

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/TabBar/TabBar.tsx
git commit -m "feat(TabBar): add overflow dropdown menu with ResizeObserver"
```

---

### Task 7: ChatArea — 集成 TabBar

**Files:**
- Modify: `frontend/src/components/Chat/ChatArea.tsx`

- [ ] **Step 1: 重写 ChatArea 使用 TabBar 和多会话消息缓存**

```typescript
import { App } from '../../../bindings/monika'
import { useStore } from '../../store'
import TabBar from '../TabBar/TabBar'
import MessageBubble from './MessageBubble'
import ChatInput from './ChatInput'

function ChatArea() {
  const messages = useStore((s) => s.messages)
  const generatingSessionId = useStore((s) => s.generatingSessionId)
  const addMessage = useStore((s) => s.addMessage)
  const clearMessages = useStore((s) => s.clearMessages)
  const projectPath = useStore((s) => s.projectPath)
  const activeSessionId = useStore((s) => s.activeSessionId)
  const openSessions = useStore((s) => s.openSessions)
  const openSessionTab = useStore((s) => s.openSessionTab)
  const closeSessionTab = useStore((s) => s.closeSessionTab)
  const switchSessionTab = useStore((s) => s.switchSessionTab)
  const setGeneratingSessionId = useStore((s) => s.setGeneratingSessionId)

  const sessionTabs = openSessions.map((s) => ({
    key: s.id,
    label: s.title || 'Untitled',
    status: (generatingSessionId === s.id ? 'generating' as const : 'idle' as const),
  }))

  const handleSend = async (text: string) => {
    if (!text.trim()) return

    if (text.startsWith('/')) {
      const cmd = text.slice(1)
      if (cmd === 'help')
        addMessage({ id: crypto.randomUUID(), role: 'system', content: 'Commands: /help /clear /exit' })
      if (cmd === 'clear') clearMessages()
      return
    }

    if (!projectPath || !activeSessionId) {
      addMessage({ id: crypto.randomUUID(), role: 'error', content: 'No project or session selected. Use /open to open a project.' })
      return
    }

    if (generatingSessionId !== '') {
      addMessage({ id: crypto.randomUUID(), role: 'error', content: 'Another session is generating. Please wait.' })
      return
    }

    addMessage({ id: crypto.randomUUID(), role: 'user', content: text })
    addMessage({ id: crypto.randomUUID(), role: 'assistant', content: '' })
    setGeneratingSessionId(activeSessionId)

    try {
      await App.SendMessage(projectPath, activeSessionId, text)
    } catch (err) {
      addMessage({ id: crypto.randomUUID(), role: 'error', content: String(err) })
      setGeneratingSessionId('' as any)
      // 使用空字符串清除 — 因为 setGeneratingSessionId 接受 string，但 '' 等同于清除
      useStore.getState().clearGeneratingSessionId()
    }
  }

  return (
    <div className="flex flex-col h-full bg-[var(--bg-main)]">
      <TabBar
        tabs={sessionTabs}
        activeKey={activeSessionId}
        onSelect={(key) => switchSessionTab(key)}
        onClose={(key) => closeSessionTab(key)}
        emptyLabel="No sessions open. Create one from the sidebar."
      />
      <div className="flex-1 overflow-y-auto p-[5px]">
        {messages.length === 0 ? (
          <div className="flex items-center justify-center h-full text-[var(--text-dim)] text-[13px]">
            No messages yet. Start a conversation.
          </div>
        ) : (
          messages.map((msg) => (
            <MessageBubble key={msg.id} message={msg} />
          ))
        )}
      </div>
      <ChatInput
        key={activeSessionId}
        onSend={handleSend}
        disabled={generatingSessionId !== ''}
      />
    </div>
  )
}

export default ChatArea
```

- [ ] **Step 2: 验证编译**

Run: `cd frontend && npx tsc --noEmit`

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/Chat/ChatArea.tsx
git commit -m "feat(ChatArea): integrate TabBar with multi-session message caching"
```

---

### Task 8: SessionList — 改为调用 openSessionTab

**Files:**
- Modify: `frontend/src/components/Sidebar/SessionList.tsx`

- [ ] **Step 1: 更新 handleSelect 和 imports**

在 store 解构中替换 `setActiveSessionTitle` 和 `setMessages` 的用法：

```typescript
// 替换现有 store 解构：
const openSessionTab = useStore((s) => s.openSessionTab)
// 移除 setActiveSessionTitle 读取（如果有的话）
// 移除 setMessages 读取
```

`handleSelect` 函数改为：

```typescript
const handleSelect = async (id: string) => {
  const session = sessions.find((s) => s.id === id)
  const title = session?.title || 'Untitled'
  await openSessionTab(id, title)
}
```

移除 `handleSelect` 中原来的 `setActiveSessionId`、`setActiveSessionTitle`、`App.LoadSession`、`setMessages` 调用——全部委托给 `openSessionTab` store action。

`handleNewSession` 中：

```typescript
const handleNewSession = async () => {
  if (!projectPath) return
  try {
    const info = await App.NewSession(projectPath)
    setSessions((prev) => [info, ...prev])
    await openSessionTab(info.id, info.title || 'Untitled')
  } catch (err) {
    console.error('Failed to create session:', err)
  }
}
```

删除操作后，仍需更新 sessions 列表：

```typescript
// handleDeleteConfirm 中，删除成功后：
} catch {
  // 原逻辑不变，但用 openSessionTab 替代手动 setMessages
  if (sorted.length > 0) {
    await openSessionTab(nearest.id, nearest.title || 'Untitled')
  } else {
    setActiveSessionId('')
    // openSessionTab 不用于空，保持手动清空
    setMessages([])
  }
}
```

更精确的修改——`handleDeleteConfirm` 中删除后的自动切换改为：

```typescript
if (sorted.length > 0) {
  const nearest = sorted[0]
  await openSessionTab(nearest.id, nearest.title || 'Untitled')
  document.getElementById(`session-${nearest.id}`)?.focus()
} else {
  setMessages([])
  setActiveSessionId('')
  document.getElementById('new-session-btn')?.focus()
}
```

- [ ] **Step 2: 验证编译**

Run: `cd frontend && npx tsc --noEmit`

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/Sidebar/SessionList.tsx
git commit -m "refactor(SessionList): delegate session loading to openSessionTab store action"
```

---

### Task 9: ConfirmModal — 支持自定义确认按钮文本

**Files:**
- Modify: `frontend/src/components/Chat/ConfirmModal.tsx`

- [ ] **Step 1: 添加 confirmLabel prop**

在 `ConfirmModalProps` 接口中添加可选 `confirmLabel` 字段，按钮文字从硬编码的 "Delete" 改为使用 prop。

```typescript
interface ConfirmModalProps {
  title: string
  message: string
  confirmLabel?: string    // 新增
  onConfirm: () => Promise<void>
  onCancel: () => void
}

// 在函数签名中解构：
function ConfirmModal({ title, message, confirmLabel, onConfirm, onCancel }: ConfirmModalProps) {
```

确认按钮文字改为：

```typescript
{isLoading ? (confirmLabel ? `${confirmLabel}...` : 'Deleting...') : (confirmLabel || 'Delete')}
```

- [ ] **Step 2: 验证编译**

Run: `cd frontend && npx tsc --noEmit`

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/Chat/ConfirmModal.tsx
git commit -m "feat(ConfirmModal): add configurable confirm button label"
```

---

### Task 10: FileEditor — 多文件 DOM 容器 + CodeMirror 缓存

**Files:**
- Modify: `frontend/src/components/FileTree/FileEditor.tsx`

- [ ] **Step 1: 重写 FileEditor 支持多文件标签和编辑器缓存**

```typescript
import { useEffect, useRef, useCallback } from 'react'
import { EditorState } from '@codemirror/state'
import { EditorView, keymap } from '@codemirror/view'
import { defaultKeymap } from '@codemirror/commands'
import { oneDark } from '@codemirror/theme-one-dark'
import { javascript } from '@codemirror/lang-javascript'
import { python } from '@codemirror/lang-python'
import { json } from '@codemirror/lang-json'
import { go } from '@codemirror/lang-go'
import { App } from '../../../bindings/monika'
import { useStore } from '../../store'
import TabBar from '../TabBar/TabBar'
import ConfirmModal from '../Chat/ConfirmModal'

const MAX_CACHED_EDITORS = 10

function getLangExtension(filePath: string) {
  if (filePath.endsWith('.go')) return go()
  if (filePath.endsWith('.ts') || filePath.endsWith('.tsx') || filePath.endsWith('.js') || filePath.endsWith('.jsx')) return javascript()
  if (filePath.endsWith('.py')) return python()
  if (filePath.endsWith('.json')) return json()
  return []
}

function FileEditor() {
  const openFiles = useStore((s) => s.openFiles)
  const activeFilePath = useStore((s) => s.activeFilePath)
  const openFileTab = useStore((s) => s.openFileTab)
  const closeFileTab = useStore((s) => s.closeFileTab)
  const switchFileTab = useStore((s) => s.switchFileTab)
  const setFileDirty = useStore((s) => s.setFileDirty)

  // CodeMirror 实例缓存
  const editorCache = useRef<Map<string, EditorView>>(new Map())
  // LRU 访问顺序
  const lruOrder = useRef<string[]>([])
  // DOM 容器 refs
  const containerRefs = useRef<Map<string, HTMLDivElement>>(new Map())
  // 待关闭脏文件
  const [dirtyClosePath, setDirtyClosePath] = useState<string | null>(null)

  // 注册 DOM 容器的回调
  const registerContainer = useCallback((path: string, el: HTMLDivElement | null) => {
    if (el) {
      containerRefs.current.set(path, el)
    } else {
      containerRefs.current.delete(path)
    }
  }, [])

  // 创建或显示 EditorView
  useEffect(() => {
    if (!activeFilePath) return
    const container = containerRefs.current.get(activeFilePath)
    if (!container) return

    let view = editorCache.current.get(activeFilePath)
    if (!view) {
      const file = openFiles.find((f) => f.path === activeFilePath)
      const content = file?.content || ''
      const state = EditorState.create({
        doc: content,
        extensions: [
          oneDark,
          keymap.of(defaultKeymap),
          getLangExtension(activeFilePath),
          EditorView.editable.of(false),
        ],
      })
      view = new EditorView({ state, parent: container })

      // LRU 管理
      if (editorCache.current.size >= MAX_CACHED_EDITORS) {
        const oldest = lruOrder.current.shift()
        if (oldest) {
          editorCache.current.get(oldest)?.destroy()
          editorCache.current.delete(oldest)
        }
      }
      editorCache.current.set(activeFilePath, view)
    } else {
      // 切换到已有实例 — 重新挂载 DOM 并刷新布局
      if (view.dom.parentElement !== container) {
        container.appendChild(view.dom)
      }
      // CodeMirror 从 display:none 切换时需要重新测量
      view.requestMeasure()
    }

    // 更新 LRU
    lruOrder.current = lruOrder.current.filter((p) => p !== activeFilePath)
    lruOrder.current.push(activeFilePath)
  }, [activeFilePath, openFiles])

  // 关闭标签时销毁编辑器
  const handleClose = useCallback((path: string) => {
    const file = openFiles.find((f) => f.path === path)
    if (file?.isDirty) {
      setDirtyClosePath(path)
      return
    }
    const view = editorCache.current.get(path)
    if (view) {
      view.destroy()
      editorCache.current.delete(path)
    }
    lruOrder.current = lruOrder.current.filter((p) => p !== path)
    containerRefs.current.delete(path)
    closeFileTab(path)
  }, [openFiles, closeFileTab])

  // 激活已打开文件 → 重新读取最新内容
  const handleSelect = useCallback(async (path: string) => {
    if (path === activeFilePath) return
    switchFileTab(path)
    // 重新从后端读取以确保内容新鲜
    try {
      const content = await App.ReadFile(path)
      useStore.getState().updateFileContent(path, content)
    } catch {
      // 文件可能已被删除，保持缓存内容
    }
  }, [activeFilePath, switchFileTab])

  const fileTabs = openFiles.map((f) => ({
    key: f.path,
    label: f.path.split('/').pop() || f.path.split('\\').pop() || f.path,
    dirty: f.isDirty,
  }))

  if (openFiles.length === 0) {
    return (
      <div className="flex-1 flex flex-col min-w-0">
        <TabBar
          tabs={[]}
          activeKey=""
          onSelect={() => {}}
          onClose={() => {}}
          emptyLabel="Preview"
        />
        <div className="flex-1 flex items-center justify-center bg-[var(--bg-main)]">
          <span className="text-[13px] text-[var(--text-dim)]">Select a file to preview</span>
        </div>
      </div>
    )
  }

  return (
    <div className="flex-1 flex flex-col min-w-0">
      <TabBar
        tabs={fileTabs}
        activeKey={activeFilePath}
        onSelect={handleSelect}
        onClose={handleClose}
        emptyLabel="Preview"
      />
      <div className="flex-1 relative overflow-hidden">
        {openFiles.map((f) => (
          <div
            key={f.path}
            ref={(el) => registerContainer(f.path, el)}
            style={{ display: f.path === activeFilePath ? 'block' : 'none' }}
            className="absolute inset-0"
          />
        ))}
      </div>
      {dirtyClosePath && (
        <ConfirmModal
          title="Unsaved Changes"
          message={`Close ${dirtyClosePath.split('/').pop() || dirtyClosePath.split('\\').pop()} without saving?`}
          confirmLabel="Discard"
          onConfirm={async () => {
            const view = editorCache.current.get(dirtyClosePath)
            if (view) { view.destroy(); editorCache.current.delete(dirtyClosePath) }
            lruOrder.current = lruOrder.current.filter((p) => p !== dirtyClosePath)
            containerRefs.current.delete(dirtyClosePath)
            closeFileTab(dirtyClosePath)
            setDirtyClosePath(null)
          }}
          onCancel={() => setDirtyClosePath(null)}
        />
      )}
    </div>
  )
}

export default FileEditor
```

注意：需要在文件顶部添加 `useState` import，以及 `App` 和 `ConfirmModal` 的 import。

```typescript
import { useEffect, useRef, useCallback, useState } from 'react'
```

- [ ] **Step 2: 验证编译**

Run: `cd frontend && npx tsc --noEmit`

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/FileTree/FileEditor.tsx
git commit -m "feat(FileEditor): multi-file tabs with CodeMirror instance cache and dirty-close flow"
```

---

### Task 11: FileTree — 改为调用 openFileTab

**Files:**
- Modify: `frontend/src/components/FileTree/FileTree.tsx` (handleFileClick)

- [ ] **Step 1: 更新 FileTree 的 store 解构**

替换旧字段：

```typescript
// 移除 selectedFilePath, setSelectedFile
// 新增：
const openFileTab = useStore((s) => s.openFileTab)
const activeFilePath = useStore((s) => s.activeFilePath)
```

- [ ] **Step 2: 替换 handleFileClick 逻辑**

将文件点击改为调用 `openFileTab`：

```typescript
const handleFileClick = async (node: FileNode) => {
  if (node.is_dir) {
    const next = new Set(expanded)
    next.has(node.path) ? next.delete(node.path) : next.add(node.path)
    setExpanded(next)
  } else {
    try {
      const result = await App.ReadFile(projectPath, node.path)
      openFileTab(node.path, result?.content || '')
    } catch {
      openFileTab(node.path, '')
    }
  }
}
```

- [ ] **Step 3: 更新选中高亮条件**

将 `isSelected` 从 `selectedFilePath === node.path` 改为 `activeFilePath === node.path`：

```typescript
const isSelected = activeFilePath === node.path
```

- [ ] **Step 4: 验证编译**

Run: `cd frontend && npx tsc --noEmit`

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/FileTree/FileTree.tsx
git commit -m "refactor(FileTree): delegate file opening to openFileTab store action"
```

---

### Task 12: 集成验证和端到端构建测试

**Files:**
- 无新建文件

- [ ] **Step 1: TypeScript 类型检查**

Run: `cd frontend && npx tsc --noEmit`
Expected: 0 errors

- [ ] **Step 2: Go 测试**

Run: `go test ./...`
Expected: 所有已有测试通过

- [ ] **Step 3: 前端构建**

Run: `cd frontend && npm run build`
Expected: build 成功

- [ ] **Step 4: Go 构建（含嵌入前端）**

Run: `go build .`
Expected: 构建成功

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "chore: verify full build passes after multi-tab integration"
```

---

### Task 13: 手动功能验证清单

- [ ] **Step 1: 启动开发模式**

Run: `wails3 dev`

- [ ] **Step 2: 验证会话标签**
  - 点击 SessionList 中的会话 → 新标签出现在 ChatArea 顶部
  - 再次点击同一会话 → 激活已有标签（不重复）
  - 点击不同会话 → 新标签，消息区切换
  - 关闭标签 → 切换到相邻标签
  - 关闭所有标签 → 显示空状态

- [ ] **Step 3: 验证文件标签**
  - 点击 FileTree 中的文件 → 新标签出现在 FileEditor 顶部
  - 点击已打开文件 → 激活已有标签，编辑器显示最新内容
  - 关闭标签 → 切换到相邻标签
  - 关闭所有标签 → 显示 "Select a file to preview"

- [ ] **Step 4: 验证流式生成**
  - 在会话中发送消息 → 标签显示 pulsing dot
  - 切换到另一个会话 → 后台生成继续
  - 切回生成中的会话 → 实时看到流式内容
  - 生成完成 → 标签显示 ✓

- [ ] **Step 5: 验证溢出下拉**
  - 打开 6+ 个标签 → `▼` 下拉按钮出现
  - 点击 `▼` → 菜单显示溢出标签
  - 选中列表项 → 激活对应标签，菜单关闭

- [ ] **Step 6: 验证键盘导航**
  - Tab 进入标签栏 → Left/Right 移动焦点
  - Enter/Space → 激活标签
  - Ctrl+W → 关闭标签
