# Change Log

本文件用于记录每次功能或行为变更，方便后续审查。

## 变更记录规则

每次提交或一组相关提交，**必须**在此文件中追加一条记录。记录格式：

```
- **简短描述**：说明做了什么、为什么做、影响范围。
```

- 每条记录一行，以 `- ` 开头，加粗简短标题，冒号后详细说明。
- 按日期分组，日期格式 `## YYYY-MM-DD`。
- 变更涉及多个模块时，在说明中分别提及。

---

## 2026-05-04

- **修复 TodoPanel 不渲染任务**：`EmitTaskEvent` 发送的事件类型为 `"task_update"` 但前端 `setupWailsEvents` 监听的是 `"task_updated"`（末尾少了一个 `d`），导致 `setSessionTasks` 从未被调用，Zustand store 中 tasks 始终为空，TodoPanel 读取 `store.tasks[activeSessionId]` 始终拿到 `undefined`。修改 `internal/api/app.go:565` 中 `Type` 字段为 `"task_updated"` 与前端对齐。

## 2026-05-01

- **中断生成**：ChatInput 右下角新增发送/停止切换按钮——发送时显示箭头图标 `IconArrowUp`，生成中切换为方形停止图标 `IconStop`，按 ESC 键停止。ChatArea 通过 `App.CancelGeneration(generatingSessionId)` 调用后端 `context` 取消中断 agent loop。Store `error` 事件识别 `cancelled` 类型时静默清除状态（不追加错误消息），后端取消时重置 session 为 idle，goroutine 检测 `ctx.Err()` 避免覆盖。设计文档：`docs/superpowers/specs/2026-05-01-interrupt-generation-design.md`、`docs/superpowers/plans/2026-05-01-interrupt-generation.md`。
- **字体打包**：将 Maple Mono NF（等宽编程字体）和 LXGW WenKai（霞鹜文楷，UI 字体）打包进前端 `src/assets/fonts/`，添加 `@font-face` 声明（`font-display: swap`），确保开箱即用无需系统安装。CodeMirror 编辑器通过 `EditorView.theme` 应用 Maple Mono NF。
- **Thinking 块可折叠**：MessageBubble 中 thinking 内容默认折叠，点击标题栏展开/收起，精简长推理内容的展示。
- **Tool 块可折叠**：Tool 卡片新增 chevron 折叠/展开切换，默认展开，便于查看或隐藏工具调用详情。
- **消息气泡响应式宽度**：MsgBlock 宽度改为 `w-full`、移除 `max-w` 上限，适配不同窗口尺寸。
- **标签颜色微调**：降低 thinking 和 tool 标签文字亮度，减少视觉干扰。
- **Icon 组件增强**：`Icon` 和 `IconChevronDown` 支持 `style` prop，允许调用方覆盖尺寸/颜色等内联样式。
- **模块化 System Prompt**：将 `BuiltinSystemPrompt`（24 行，仅 tool usage）替换为 6 个模块常量（`PromptIdentity`、`PromptToolUsage`、`PromptCodeQuality`、`PromptResponseStyle`、`PromptSafetyBoundaries`、`PromptRemember`），遵循 U 型注意力曲线排列（关键规则在首尾，近因效应强化）。参考 Claude Code 和 OpenCode 的 prompt engineering 模式，增加代码质量原则（YAGNI、精简注释、系统边界验证）、安全边界（路径安全、破坏性操作确认、git 保护）、响应风格（简洁输出、无 emoji、语言匹配）等通用规则。工具描述增强（bash/grep/glob/file_write），增加安全约束和专用工具偏好引导。新增 `system_prompt_test.go` 守卫测试（常量非空、token 预算 < 8000 字符）。设计文档：`docs/superpowers/specs/2026-05-01-system-prompt-optimization-design.md`、`docs/superpowers/plans/2026-05-01-system-prompt-optimization.md`。
- **Tiktoken 精确 Token 计算**：新增 `pkg/tokenizer/` 包，使用 `tiktoken-go`（cl100k_base 编码）进行客户端 token 估算，作为 API 未返回 usage 数据时的 fallback。扩展 `engine.Usage` 新增 `ReasoningTokens`、`CacheReadTokens`、`CacheWriteTokens` 字段和 `ContextTokens()` 方法。`openai.StreamChat` 增加 `stream_options.include_usage` 确保 OpenAI 流式响应返回 usage 数据，并解析 `completion_tokens_details` 和 `prompt_tokens_details`。Agent Loop 新增 `modelContextLimits` context 窗口映射和 `estimateContextTokens()` 预估算方法（首轮发出预估 usage 事件）。前端 StatusBar 改为 `tok: 1.2K / 128K` 格式展示实际值 / 模型上限，session 切换时重置 token 计数。参考 OpenCode 的 `Token.estimate()` 和 `getUsage()` 模式。
- **Session 状态指示器**：Session 列表每行新增状态指示器——生成中显示黄色旋转圆圈动效 (`@keyframes spin`)，完成后绿色实心圆点，失败时红色实心圆点（悬停 tooltip 显示错误信息），空闲 session 无指示器。后端 `Session`/`SessionInfo` 结构体新增 `Status` 字段（`idle`/`generating`/`success`/`failure`），`SessionManager` 新增 `SetStatus` 方法，`Load()` 内建旧文件兼容（空 status → idle），`SendMessage` goroutine 管理 status 生命周期，`CancelGeneration` 重置为 idle，启动时 `resetStaleSessions` 清理崩溃残留的 generating 状态，新增并发防重入守卫。前端 Zustand store 新增 `sessionStatuses`/`sessionErrors` 实时追踪，`StatusDot` 组件带无障碍支持（`role="status"`、`aria-label`、红色 dot 附带 `title` tooltip）。设计文档：`docs/superpowers/specs/2026-05-01-session-status-indicator-design.md`、`docs/superpowers/plans/2026-05-01-session-status-indicator.md`。
- **模型选择器**：ChatInput 上方新增 `ChatInputToolbar`，含 `ModelSelector` 下拉菜单，展示可用模型列表并支持切换。后端新增 `GetModels` API 返回 `provider.ListModels()` 结果，`SendMessage` 接受 `model` 参数透传至 agent loop。Zustand store 新增 `availableModels`/`selectedModel`/`loadModels`。设计文档：`docs/superpowers/specs/2026-05-01-model-selector-design.md`、`docs/superpowers/plans/2026-05-01-model-selector.md`。
- **修复 StatusDot 状态解析和 NewSession 绑定**：`getStatus` 改用 `!= null` 显式检查替代 falsey 判断，防止空字符串绕过内存状态。`NewSession` 的 TS 绑定移除多余的 `model` 参数，与 Go 签名 `NewSession(projectPath string)` 对齐。
- **审查修复：取消竞态与状态同步**：SendMessage goroutine 取消后分支化 Save（避免覆写 CancelGeneration 的 idle 状态），`ctx.Err() == nil` 守卫 `session_updated` 事件发送。前端 `cancelled` 错误路径补全 `setSessionStatus(idle)` + `clearSessionError` + `bumpSessionListVersion`，防止侧边栏永远显示 spinner。关闭标签页时自动调用 `CancelGeneration` 清理后端 goroutine。
- **审查修复：模型状态健壮性**：ChatArea 新增 `selectedModel` 空值校验（无模型时不发送）。`loadModels` 验证旧 `selectedModel` 是否仍在新的 `availableModels` 列表中，不在则回退到第一个。`resetProjectState` 清除 `availableModels`/`selectedModel` 防止跨项目残留。新增 `setSelectedModel` store action 替代 `ChatInputToolbar` 中直接 `useStore.setState`。
- **审查修复：ESC 稳定性与状态常量化**：ChatInput ESC 监听器改用 `useRef(onStop)` 稳定引用，依赖数组简化为 `[disabled]` 消除每次渲染重注册。Go 端新增 `StatusIdle`/`StatusGenerating`/`StatusSuccess`/`StatusFailure` 常量，替换 `app.go` 和 `session_manager.go` 中所有硬编码字符串。
- **TabBar ResizeObserver 性能优化**：使用 `tabsRef` + `calcRef` 稳定引用避免每次 tabs 变化重建 ResizeObserver，`setOverflowKeys` 改用函数式更新减少不必要的重渲染。
- **文件面板改进**：Edit/Diff 切换按钮从 FileEditor 移至 StatusBar（新增 `IconCode` 图标），支持 `Ctrl+/` 快捷键。FileTree git 状态显示优化——`D`(红)/`A`(绿)/`M`(黄)/`R`(紫)/`??`(灰) 颜色区分与简洁标签。新增刷新按钮 + 窗口聚焦自动刷新文件树。修复 `internal/api/file_service.go` 中 Windows 反斜杠路径无法匹配 git status 的问题（`filepath.ToSlash`）。
- **ChatInput 重构**：`ModelSelect` 下拉和 token 显示内联到 ChatInput 底部栏，移除 `ChatArea` 中 `ChatInputToolbar` 的引用。文本框 `auto-resize`（最大高度 160px）。发送按钮改为纸飞机图标，停止按钮改为无背景方形图标。
- **Last project 持久化**：`internal/api/app.go` 新增 `~/.monika/state.json` 读写（原子写入 via tmp+rename），启动时自动恢复上次打开的项目。`GetCurrentProject` 优先返回 last project 而非 startupCwd。
- **NewSession model 参数**：`frontend/bindings/monika/index.ts` 中 `NewSession` 签名新增 `model` 参数，`SessionList` 创建 session 时透传 `selectedModel`。
- **分支冲突 AI 修复**：`BranchDropdown` 和 `CreateBranchPanel` 切换/创建分支时检测 unmerged 冲突，弹出 `ConfirmModal` 提供"Let AI Handle"选项（`resolveUnmergedWithAI`）。`dropdownHelpers.ts` 新增 `parseUnmergedError` 解析错误信息中的冲突文件列表。移除残留 `console.log` 调试输出。

## 2026-04-30

- **CreateBranchPanel z-index 修复**：TitleBar 的 `backdrop-blur-md`（`backdrop-filter`）创建了新的 CSS stacking context，导致 `position: fixed` 的 CreateBranchPanel 被侧边栏遮盖。修复：给 TitleBar 添加 `position: relative; z-index: 10`，将其 stacking context 提升到主内容区之上。同时记录 z-index 规范和 portal 优先原则到 AGENTS.md。
- **Wails API 参数修复**：`bindings/monika/index.ts` 中 `Call.ByName` 的参数传递从数组包裹改为位置参数（`Call.ByName(name, arg1, arg2, ...)` 而非 `Call.ByName(name, [arg1, arg2, ...])`），修复 `ListBranches`、`CreateBranch`、`SwitchBranch`、`ListDirectory` 调用失败。
- **前端防御性修复**：SessionList 和 FileTree 的 `useEffect` 添加 cancelled 标志防止卸载后 setState；`useMemo` 中添加 `Array.isArray` 守卫防止 `undefined.sort()` 崩溃。
- **Go 返回值安全修复**：`FileService.ListDir` 和 `SessionManager.List` 在目录不存在或为空时返回 `[]T{}` 而非 `nil`；`FileService.ListChanges` 预分配 `make([]FileChange, 0)`。
- **文档同步**：README.md 更新 features（layout modes、project/branch management、multi-tab sessions、worktree）；AGENTS.md 设计令牌表、布局图、z-index 规则、TitleBar 规范全部与当前代码对齐。

## 2026-04-29

- **项目/分支切换功能**：新增 `ProjectDropdown`（最近项目列表、打开文件夹入口）、`BranchDropdown`（本地/远程分支列表、分支切换）、`CreateBranchPanel`（从指定分支创建并切换新分支）、`FileDialog`（应用内文件夹选择器）。全部集成到 TitleBar。后端新增 `OpenProject`（扫描 git worktrees）、`ListBranches`、`SwitchBranch`、`CreateBranch`（含分支名校验）、`GetRecentProjects`（原子 JSON 读写）。设计文档：`docs/superpowers/specs/2026-04-28-project-branch-switching-design.md`。
- **代码审查修复**：P0/P1/P2 问题修复 — 路径规范化、并发安全、错误处理增强。提取共享 helper（`SectionHeader`、dropdown 样式、错误/脏文件提示消息）。
- **性能优化**：移除冗余锁、并行化 I/O 操作、去重逻辑简化。

## 2026-04-28

- **多标签页支持**：ChatArea 支持同时打开多个会话，FileEditor 支持同时打开多个文件，各自通过独立 TabBar 管理。新增 `TabBar` 组件（标签栏、溢出下拉、状态指示、键盘导航、ARIA 无障碍）。Store 重构：新增 `openSessions`、`sessionMessages`、`openFiles`、`activeFilePath`、`generatingSessionId` 等 11 个字段和 14 个操作，移除旧单视图字段。流式事件按 `session_id` 路由到 per-session 缓存。FileEditor 改为多 DOM 容器 + CodeMirror 实例缓存（LRU 淘汰，上限 10）。SessionList 委托 `openSessionTab`，FileTree 委托 `openFileTab`。四轮代码审查修复 8 个 P0/P1 和 7 个 P2/P3 问题。设计文档：`docs/superpowers/specs/2026-04-28-multi-tab-design.md`。
- **ConfirmModal 增强**：新增 `confirmLabel` prop 支持自定义确认按钮文本，通用错误文案从 "Deletion failed" 改为 "Operation failed"。
- **FileEditor 滚动条修复**：移除 `overflow-hidden` 添加 `.cm-editor { height: 100% }` 和 `.cm-scroller::-webkit-scrollbar` 显式样式，确保 CodeMirror 滚动条与全局样式一致。
- **TitleBar 关闭按钮修复**：`Application.Quit()` 替换为 `Window.Close()`，解决 Wails v3 alpha.78 中无法完全关闭窗口的问题。
- **AGENTS.md 更新**：新增 TabBar、ChatArea、FileEditor 组件规范，更新 Zustand store 状态字段说明，添加中文沟通语言规则。
- **Split Screen 三栏布局**：重构 App 布局为 ChatGroup / DragDivider / FilesGroup 三区域，支持 chat、split、files 三种模式，TitleBar 布局切换按钮切换。CSS show/hide（不卸载组件）保持状态。Zustand store 新增 `layoutMode`、`splitRatio` 字段。设计文档：`docs/superpowers/specs/2026-04-28-split-screen-layout-design.md`。
- **DragDivider 可拖拽分割线**：新增 `DragDivider` 组件，4px 宽，mousedown/mousemove/mouseup 处理，splitRatio 范围 0.2–0.8，拖拽时 `user-select: none`。
- **FileEditor 组件抽离**：从 FileTree 中独立为 FileTree/FileEditor，通过 Zustand store 通信（`selectedFilePath`、`selectedFileContent`），FileTree 写、FileEditor 读。空状态展示 "Preview" 占位。添加一致的 header bar（文件名 + 关闭按钮）。
- **ChatArea header bar**：顶部显示 session title（从 Zustand `activeSessionTitle` 读取），关闭按钮关闭当前 session（`setActiveSessionId('')`）。样式与 FileEditor header bar 一致。
- **SessionList session title 同步**：选择或新建 session 时同步写入 `activeSessionTitle` 到 store。
- **Session 删除功能**：SessionList 每行 hover/focus 时显示删除图标，点击触发 `ConfirmModal` 确认弹窗。删除后自动切换到最近更新的剩余 session（按 `updated_at` 降序），无剩余时清空消息。支持键盘 Delete/Backspace 触发删除。设计文档：`docs/superpowers/specs/2026-04-27-session-delete-and-collapse-design.md`。
- **ConfirmModal 确认弹窗**：新增 `ConfirmModal` 组件（`role="alertdialog"`），支持 loading / error 状态、焦点陷阱（Tab/Shift+Tab 在 Cancel/Delete 间循环）、Escape 关闭、backdrop 点击关闭。loading 期间禁用所有关闭操作。Portal 渲染到 document.body。
- **StatusBar 图标化**：文本标签替换为 `<IconConsole>`、`<IconFile>`、`<IconSidebar>` 图标按钮，侧边栏和文件树可独立切换显示。新增状态指示器图标。
- **ChatInput session 切换清空**：通过 React `key={activeSessionId}` prop 在 session 切换时卸载/重新挂载 ChatInput，自动清除输入草稿。
- **Glassmorphism 设计系统**：全面替换 opaque 主题为 glass 面板效果，新增 `--glass-*` 和 `--glass-rgb` CSS 自定义属性，应用至 ChatArea 输入框、消息气泡、FileTree、FileEditor、Console。TitleBar 窗口控制按钮改为 icon-only。
- **Taskfile 构建流水线**：新增 `Taskfile.yml`，支持 `task build`（跨平台构建）、`task dev`（wails3 dev）、`task check`（go test + go vet + tsc --noEmit）。
- **自动切换副作用修复**：将 auto-switch 副作用从 Zustand setter 中提取到事件处理层，消除 state updater 中的不可预期行为。
- **图标扩展**：新增 `IconTrash`、`IconSidebar`、`IconConsole`、`IconChatLayout`、`IconSplitLayout`、`IconFilesLayout`。

## 2026-04-27

- **从 TUI 迁移到 Wails v3 桌面 GUI**：新增 `internal/api/`（App、EventBus、SessionManager、FileService）、React 前端（TitleBar、SessionList、ChatArea、FileTree、Console、StatusBar）、CodeMirror 6 编辑器、Zustand 状态管理、`internal/bootstrap/` provider 初始化。移除 `cmd/monika/`（CLI/REPL/TUI）、`internal/session/`（旧 session）、Bubble Tea/cobra/go-prompt 依赖。入口改为根目录 `main.go`。
- **前端设计系统 Cool Dark**：基于 VS Code Dark+ 风格，建立完整 CSS 自定义属性设计 token（`--bg-main`、`--bg-sidebar`、`--text-primary`、`--accent` 等 20+ 变量）、字体系统（`--font-ui` / `--font-mono`）、z-index 规范。写入 `AGENTS.md` Frontend Conventions。
- **ChatArea 卡片重设计**：消息列表改为 card 样式、thinking 内容折叠展示、session 历史消息通过 Go API 加载、MessageBubble 和 ToolCard 组件化。
- **GitHub 风格 Markdown 渲染**：新增 markdown 渲染（代码块语法高亮、表格、列表），ToolCard 输出改进（折叠长输出、状态颜色编码）。
- **代码审查修复**：修复 session 并发竞态（SessionManager mutex）、cancelFuncs 并发访问（cancelMu）、JSON 序列化字段名不匹配（ToolEvent/UsageEvent json tags）、App map 并发访问（sync.RWMutex）、前端空白参数传递、SessionList 接入 Go API、StatusBar 接入 Zustand、Console 事件监听器泄漏修复、窗口控制按钮接入 Wails runtime。
- **工具注册入口统一**：工具注册移入 `internal/tool/builtin/register.go`，`internal/tool/tool.go` 保持纯接口定义。
- **移除 CLI/REPL 模式**：清理 `cmd/monika/` 目录，仅保留 Wails 桌面应用入口，`main.go` 直接嵌入前端 dist。
- **dev 模式修复**：`//go:embed` 改为 `os.DirFS`，解决 dev 模式下 `frontend/dist` 不存在的构建失败问题。
- **Session 删除设计文档**：新增 session delete + sidebar collapse 设计 spec 及三轮 review 修复（loading/error 状态、无障碍、键盘访问、焦点管理）。设计文档：`docs/superpowers/specs/2026-04-27-session-delete-and-collapse-design.md`。

## 2026-04-26

- **工具调用闭环**：实现完整的 Tool calling 模块，包含 Tool 接口、ToolRegistry、6 个内置工具（file_read/write/list、glob、grep、bash），多平台 bash 支持（Win: pwsh/ps/cmd，Unix: sh/bash），所有文件工具强制项目目录路径校验。
- **AgentLoop 多轮循环**：替换单轮 AgentOption 为多轮 AgentLoop，支持工具调用循环（最大 25 轮），confirmFn 确认回调，token usage 跨轮累积。
- **SSE 工具调用解析**：扩展 `pkg/openai` 支持 tool_calls 分块解析与参数累加，ToolCall 线格式序列化（type/function 嵌套），DeepSeek reasoning_content 透传。
- **ChatMessage 扩展**：增加 ToolCalls、ToolCallID、Name、ReasoningContent 字段。
- **CLI 增强**：chat 命令自动加载 AGENTS.md 作为 system prompt，注册内置工具。
- **测试覆盖**：新增 internal/tool、builtin、agent loop、SSE tool_calls 全套测试。
- **交互模式与会话持久化**：新增 REPL 模式支持多轮对话、会话保存与恢复（后续已迁移至 Wails GUI）。
- **引擎插件架构**：新增 `pkg/engine` 接口定义 + registry、provider adapter 抽象层、deepseek/openai 引擎适配器、skill/MCP 引擎。
- **TUI REPL**：新增 Bubble Tea TUI 提供交互式聊天界面（后续已迁移至 Wails GUI）。
- **Wails v3 前端 scaffold**：新增 React + TypeScript + Tailwind CSS v4 + Vite 前端脚手架，Wails App/EventBus/SessionManager/FileService Go 服务绑定。
- **单模块回退**：从 go.work 多模块实验回退到单模块 `go.mod` + `pkg/` + `internal/` 布局。

## 2026-04-25

- **Provider 引擎插件体系**：建立 `internal/engines/provider/` 适配器层 + `pkg/engine/` 公共接口 + 注册表，支持 deepseek/openai 后端。废弃 go-plugin/gRPC，采用 `database/sql` 风格 `init()` 自注册。
- **分层 YAML 配置**：实现 `internal/config/` 全局（`~/.monika/config.yaml`）+ 项目（`.monika/config.yaml`）深度合并加载，支持 `model_providers` / `skill` / `mcp` / `tools` 配置段。
- **项目结构重组**：目录重构为 `internal/`（agent、config、engines、tool）+ `pkg/`（engine、openai）+ `cmd/monika/`（CLI 入口），遵循 Go 标准布局。
- **AGENTS.md 初始化**：创建仓库文档，记录结构、验证命令和注意事项。
- **change.log 初始化**：建立变更记录文件。
